1. Run `bower install`
2. Run `r.js -o build.js`

Now open the file:
`index.html` in browser, it works!

But opening compiled min.html in browser:
`index.min.html` it doesn't work, `Backbone` is not defined. :(
